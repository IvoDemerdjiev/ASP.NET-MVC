@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div class="col-md-6 margin-top-10">
        <a href="#" class="btn btn-info btn-block">
            <span class="glyphicon glyphicon-arrow-down"></span> Withdrawal
        </a>
        <a href="#" class="btn btn-info btn-block">
            <span class="glyphicon glyphicon-flash"></span> Quick cash 100
        </a>
        <a href="@Url.Action("Details", "CheckingAccount")" class="btn btn-info btn-block">
            <span class="glyphicon glyphicon-question-sign"></span> Balance Inquery
        </a>
    </div>
    <div class="col-md-6 margin-top-10">
        <a href="#" class="btn btn-info btn-block">
            <span class="glyphicon glyphicon-print"></span> Print Statement
        </a>
        <a href="#" class="btn btn-info btn-block">
            <span class="glyphicon glyphicon-transfer"></span> Transfer Funds
        </a>
        <a href="@Url.Action("Deposit", "Transaction", new { checkingAccountId = ViewBag.CheckingAccountId})" class="btn btn-info btn-block">
            <span class="glyphicon glyphicon-arrow-up"></span> Deposit
        </a>
    </div>
</div>
